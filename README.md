# alert-iot

Run the following script for installing environment and dependencies:

Linux version: 
```bash
python -m venv venv

source venv/bin/activate # for linux

pip install numpy librosa tensorflow scipy
```

Window version:
```shell
python -m venv venv

venv\Scripts\activate # for window

pip install numpy librosa tensorflow scipy
```